# Анализ и прогнозирование ценообразования на образовательные услуги

**Автор:** Горячевская Екатерина Николевна<br>
**Тема ВКР:** «Анализ и прогнозирование ценообразования на образовательные услуги»

[![Открыть в Google Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/Kathrin-23/muiv_costs/blob/main/ml/train_model.ipynb)

Веб-приложение на Flask загружает и анализирует сведения о стоимости
образовательных услуг, сравнивает модели машинного обучения и сохраняет прогнозы
с указанием алгоритма, который их сформировал.

## Данные и воспроизводимость

Синтетический набор заменен воспроизводимой сборкой из проверяемых открытых
источников:

- детальные таблицы исследования НИУ ВШЭ «Мониторинг качества приема в вузы»
  за 2018–2024 годы;
- открытый API Всемирного банка с индексом потребительских цен РФ.

Подробное происхождение и правила расчета производных признаков описаны в
[`data/SOURCES.md`](data/SOURCES.md). Каждая строка исходного файла содержит URL
источника и номер исходной записи.

Повторная сборка:

```bash
python data/build_dataset.py
```

Команда формирует:

- `data/raw/educational_services_prices.csv` — исходный набор с полями
  происхождения;
- `data/processed/prepared_dataset.csv` — очищенная таблица для ML.

В текущей версии используется **24 164 реальных наблюдения** по 819
образовательным организациям и 66 укрупненным группам направлений за 2019–2024
годы. Обработанный набор имеет размер 24 164 × 9. Случайная генерация и
искусственное размножение строк отсутствуют.

`competitor_price` — медиана фактических цен **других вузов** того же года и
направления. Цена текущего вуза исключается из расчета, поэтому целевая
переменная строки не попадает в признак.

## Google Colab

Ноутбук [`ml/train_model.ipynb`](ml/train_model.ipynb) предназначен для запуска
непосредственно в Google Colab и содержит сохраненные результаты выполненных
ячеек. Он отдельно показывает:

1. подтверждение среды Colab;
2. загрузку CSV по URL из GitHub;
3. очистку и проверку качества;
4. исследовательский анализ и графики;
5. временное разбиение: train 2019–2022, validation 2023, test 2024;
6. GridSearchCV для трех моделей;
7. пятикратную кросс-валидацию `TimeSeriesSplit`;
8. общую сравнительную таблицу;
9. графики фактических и прогнозных цен, остатков и ошибок;
10. важность признаков и вывод о переобучении;
11. отдельное сохранение моделей, метрик и реестра.

Для запуска нажмите кнопку **Open in Colab** в начале README или ноутбука и
выберите в Colab команду «Среда выполнения → Выполнить все».

## Сравниваемые модели

Одна команда последовательно настраивает и обучает:

- Random Forest (`RandomForestRegressor`);
- Gradient Boosting (`GradientBoostingRegressor`);
- гребневую регрессию (`Ridge`).

```bash
python ml/train_model.py
```

Для каждой модели рассчитываются MAE, RMSE, MAPE и R² на обучающей,
валидационной и тестовой выборках. MAPE хранится и отображается в процентах.
Параметры подбираются через GridSearchCV с временными блоками, а стабильность
оценивается пятикратной `TimeSeriesSplit`. Итоговая модель
выбирается по минимальному RMSE на валидационной выборке, без использования
тестовой выборки при выборе.

Текущий эксперимент выбрал Random Forest:

| Модель | Validation RMSE | Test RMSE | Test MAPE | Test R² |
|---|---:|---:|---:|---:|
| Random Forest | 26 716,11 | 28 786,25 | 8,38% | 0,9104 |
| Gradient Boosting | 29 243,17 | 29 510,58 | 8,31% | 0,9059 |
| Ridge | 29 160,66 | 28 549,28 | 8,00% | 0,9119 |

## Артефакты обучения

Модели сохраняются независимо:

- `ml/model_random_forest.pkl`;
- `ml/model_gradient_boosting.pkl`;
- `ml/model_ridge.pkl`.

Результаты эксперимента:

- `ml/metrics_random_forest.json`;
- `ml/metrics_gradient_boosting.json`;
- `ml/metrics_ridge.json`;
- `ml/model_registry.json` — единый реестр и выбранная модель;
- `ml/model_comparison.csv` — сравнительная таблица;
- `ml/diagnostics/` — графики и JSON важности признаков.

## Загрузка набора данных

На странице «Датасеты» доступны оба способа:

- локальный CSV/XLSX;
- прямая HTTP(S)-ссылка на CSV/XLSX.

Скачивание по URL выполняется потоково с ограничением 16 МБ. После загрузки
используется общая проверка обязательных полей и очистка данных. Локальная
загрузка файла сохранена без изменений.

## Прогнозирование

На странице прогноза пользователь выбирает одну из сохраненных моделей. Название
алгоритма записывается в `ForecastResult.model_name` вместе с ценой, входными
параметрами и рекомендацией. Модель отображается в личном кабинете, отчетах и
экспортах. Для прогноза из карточки услуги автоматически используется модель,
выбранная сравнительным экспериментом.

Модель использует только восемь признаков с реальной вариативностью:

- год прогноза;
- образовательная организация;
- укрупненная группа направлений;
- последняя опубликованная цена;
- медиана цен других вузов;
- число зачисленных на платные места;
- средний балл платного приема;
- макроэкономический индекс.

Регион, рекламные расходы, скидка, искусственный индекс спроса, формат и
длительность удалены из модели и формы: открытый набор не подтверждает их
влияние.

## Быстрый запуск

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python main.py
```

В Windows:

```bat
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python main.py
```

Приложение откроется по адресу <http://127.0.0.1:5000>.

Демонстрационные учетные записи:

| Роль | Логин | Пароль |
|---|---|---|
| Администратор | `admin` | `admin123` |
| Аналитик | `analyst` | `analyst123` |
| Пользователь | `user` | `user123` |

## Тесты

```bash
python -m pytest -q
```

18 тестов проверяют локальную и URL-загрузку, объем и вариативность данных,
leave-one-out расчет конкурентов, временное разбиение, процентный MAPE,
отсутствие неподтвержденных полей в форме, сохранение трех моделей, выбор модели
при прогнозе, запись `model_name` и структуру выполненного Colab.

## Основные модули

- `app/ml_service.py` — подготовка данных, обучение, диагностика и прогноз;
- `app/file_service.py` — локальная и URL-загрузка CSV/XLSX;
- `app/routes.py` — маршруты и взаимодействие веб-интерфейса с сервисами;
- `app/models.py` — модели SQLAlchemy;
- `ml/train_model.py` — консольный запуск сравнительного эксперимента;
- `ml/predict.py` — пример прогноза автоматически выбранной моделью;
- `ml/train_model.ipynb` — полноценный Colab-эксперимент.

Технологии: Python, Flask, Flask-SQLAlchemy, pandas, scikit-learn, joblib,
Matplotlib, SQLite, openpyxl, python-docx и pytest.
